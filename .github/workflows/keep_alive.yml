name: Keep Alive

on:
  schedule:
    - cron: '0 12 */5 * *'  # every 5 days at noon UTC

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -s -o /dev/null https://fusion-annotator.streamlit.app
